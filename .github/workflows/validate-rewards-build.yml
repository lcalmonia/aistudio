name: Validate rewards build
on:
  push:
    branches: [rewards-loyalty-configurable]
    paths-ignore: ['.github/workflows/validate-rewards-build.yml']
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run build
      - if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/validate-rewards-build.yml
          git commit -m "chore: remove rewards build validation workflow"
          git push
